<div id="pre-existing" class="container">
	<h3>OBJECT</h3>
	<p><%= @email.object %> </p>
	<h3>BODY</h3>
	<p><%= @email.body %></p>
	<%= link_to "Supprimer", email_path(@email.id), class: "btn btn-danger btn-block", method: "delete", remote: true %>

	<%= link_to "Marquer comme non-lu", "#", class: "btn btn-info btn-block" %>
</div>
